apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: freshveggies-main-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: main-service
                port:
                  number: 80